{
  "name": "your-threejs-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "three": "^0.157.0" // Adjust the version as needed
    // Add other dependencies here if required
  },
  "devDependencies": {
    "vite": "^4.4.11" // Adjust the version as needed
    // Add other dev dependencies here if required
  },
  "engines": {
    "node": "18.x" // Vercel supports Node.js 14.x at the time of my last knowledge update
  },
  "license": "MIT",
  "main": "index.js",
  "private": false,
  "type": "module",
  "vite": {
    "build": {
      "outDir": "public" // The directory where Vite will output your built files
    }
  }
}
